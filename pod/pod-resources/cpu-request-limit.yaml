apiVersion: v1
kind: Pod
metadata:
  name: cpu-demo
  namespace: cpu-example
spec:
  containers:
  - name: cpu-demo-ctr
    image: rusita/cpu-stress
    resources:
      requests:
        cpu: "500m"
      limits:
        cpu: "1000m"
    args:
    - -cpus
    - "2"

